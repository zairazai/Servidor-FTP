Bitácora de desarrollo 

==22-03-2026==================================================================LINUX
05:40 pm 
=1. Preparación del entorno=====================================================

Actividades realizadas
Se creó la estructura del proyecto en el equipo real.
Se organizaron las carpetas para separar scripts, configuraciones, evidencias y documentación.
Se creó el archivo README.md con la descripción del proyecto.
Se inicializó el repositorio Git dentro de la carpeta del proyecto.
Se realizó el primer commit con la estructura base del sistema.
Se configuró el repositorio remoto en GitHub.
Se enlazó el repositorio local con el remoto.
Se realizó el primer push al repositorio en GitHub.
Se verificó que los archivos se sincronizaran correctamente en el repositorio remoto.

=evidencias==================================================================================================

Captura de la estructura de carpetas del proyecto.
Captura del comando git init.
Captura del primer commit.
Captura del estado del repositorio (git status).
Captura del push al repositorio remoto.
Captura del repositorio en GitHub con archivos visibles.
=problemas===================================================================================================

Error al realizar el push inicial indicando que no existían commits en la rama.
Se corrigió realizando primero el commit antes de hacer el push.
=============================================================================================================
06:40 pm 

=2. Configuración de red y validación de ips====================================================================

actividades realizadas: 

Se verificó el estado inicial de las máquinas virtuales utilizadas en la práctica.
Se configuraron las interfaces de red de cada máquina virtual.
Se asignó un adaptador en modo NAT para permitir acceso a internet.
Se configuró un segundo adaptador en modo red interna con el nombre "red_sistemas".
Se aseguró que todas las máquinas virtuales utilizaran la misma red interna.
Se encendieron las máquinas virtuales para continuar con la configuración.
Se obtuvieron las direcciones IP de cada equipo utilizando comandos de sistema.
Se identificó que los servidores contaban con direcciones IP estáticas previamente configuradas.
Se detectó que el cliente no contaba con dirección IP IPv4 en la red interna.
Se configuró manualmente una dirección IP estática en el cliente mediante netplan.
Se corrigieron errores de sintaxis en el archivo de configuración YAML.
Se ajustaron los permisos del archivo de configuración de red para cumplir con las recomendaciones del sistema.
Se aplicaron los cambios de configuración de red.
Se verificó la asignación correcta de la dirección IP en el cliente.
Se realizaron pruebas de conectividad mediante comandos ping entre las máquinas virtuales.
Se confirmó la comunicación exitosa entre el cliente y los servidores.
Se configuró una carpeta compartida entre el host y las máquinas virtuales.
Se verificó el acceso a la carpeta compartida desde los distintos sistemas.

=evidencias==================================================================================================

Capturas de configuración de red en VirtualBox.
Capturas de direcciones IP en cada máquina virtual.
=problemas===================================================================================================

El cliente no obtenía dirección IP automáticamente en la red interna.
Se presentaron errores de sintaxis en el archivo de configuración de netplan debido a una incorrecta indentación.
Se generaron advertencias relacionadas con los permisos del archivo de configuración.
Se observó que el sistema utilizó un método alternativo para aplicar la configuración de red.
==============================================================================================================
9:00 pm 
=3. Configuración Servidor FTP en Linux   ====================================================================

actividades realizadas: 

Se instaló el servicio vsftpd en el servidor Ubuntu.
El servicio vsftpd se encontraba inactivo después de la instalación.
Se inició manualmente el servicio utilizando systemctl.
Se habilitó el servicio para ejecutarse automáticamente al iniciar el sistema.
Se verificó que el servicio se encuentra activo y en ejecución.
Se creó la estructura base de directorios para el servidor FTP.
Se crearon los grupos requeridos (reprobados y recursadores).
Se agregaron parámetros adicionales en el archivo vsftpd.conf para configurar el acceso anónimo.
Se definió la carpeta raíz del usuario anónimo.
Se habilitó el uso de chroot para restringir usuarios a su directorio.
Se configuró el parámetro allow_writeable_chroot para permitir escritura sin errores.
Se confirmó que el servicio se inicia correctamente sin errores.
Se realizó una prueba de conexión FTP desde el cliente.
Se validó el acceso anónimo al servidor FTP.

=evidencias==================================================================================================
Capturas de instalación del servicio.
Capturas del estado del servicio.
Capturas de la estructura de carpetas.
Capturas de creación de grupos.
Capturas de configuración del archivo vsftpd.conf.
Captura de acceso anonimo 

=============================================================================================================
23-03-2026 2:20 pm
=4.Configuracion de usuarios y permisos=======================================================================

actividades realizadas: 
Se creó un usuario de prueba en el sistema para validar el acceso autenticado al servidor FTP.
Se asignó el usuario a los grupos correspondientes (grupo específico y grupo general de escritura).
Se configuró la carpeta personal del usuario dentro de la estructura del servidor FTP.
Se establecieron los permisos adecuados en las carpetas general, de grupo y personal.
Se configuró una vista personalizada para el usuario dentro del servidor FTP.
Se implementaron enlaces simbólicos inicialmente para estructurar el acceso a los directorios.
Se detectó que los enlaces simbólicos no permitían la navegación dentro del servicio FTP.
Se identificó que la limitación estaba relacionada con restricciones de seguridad del servicio vsftpd al usar chroot.
Se sustituyeron los enlaces simbólicos por bind mounts para presentar los directorios como rutas reales.
Se crearon directorios físicos en la vista del usuario para montar las rutas necesarias.
Se aplicaron montajes tipo bind para las carpetas general, de grupo y personal.
Se verificó que los montajes se aplicaron correctamente mediante comandos de sistema.
Se reinició el servicio vsftpd para aplicar los cambios realizados.
Se validó el acceso autenticado al servidor FTP desde el cliente.
Se comprobó la correcta visualización de la estructura de directorios del usuario.
Se verificó la navegación entre directorios dentro del entorno FTP.
Se realizaron pruebas de escritura en las diferentes carpetas del servidor.
Se validó la transferencia de archivos desde el cliente hacia el servidor utilizando el comando put.
Se confirmó el almacenamiento correcto de los archivos dentro de las rutas correspondientes en el servidor.

=evidencias==================================================================================================
Capturas de creación y configuración del usuario.
Capturas de asignación de grupos.
Capturas de configuración de permisos en directorios.
Capturas de los comandos utilizados para implementar bind mounts.
Capturas de verificación de montajes activos.
Capturas de acceso exitoso al servidor FTP.
Capturas de transferencia de archivos mediante FTP.
Capturas de verificación de archivos en el servidor.

=problemas===================================================================================================

El usuario autenticado no podía acceder a los directorios mediante FTP.
Se presentó un error al intentar cambiar de directorio debido al uso de enlaces simbólicos.
El servicio vsftpd restringía el acceso a enlaces simbólicos en entornos con chroot habilitado.
Se identificaron restricciones relacionadas con permisos en rutas intermedias del sistema.
==============================================================================================================
9:48 pm
=4.Configuracion de usuarios y permisos=======================================================================

actividades realizadas:

Se desarrolló y ajustó un script de automatización para la instalación y configuración del servidor FTP utilizando vsftpd.
Se implementó la validación de idempotencia para evitar configuraciones duplicadas.
Se crearon grupos de usuarios (reprobados, recursadores y ftpwriters).
Se generó la estructura de directorios del servidor FTP.
Se configuraron los permisos de acceso mediante chmod, chown y grupos.
Se implementó la lógica de usuarios con vistas personalizadas mediante bind mounts.
Se permitió el acceso autenticado con permisos de escritura en carpetas específicas.
Se configuró el acceso anónimo restringido a la carpeta pública en modo solo lectura.
Se realizaron pruebas funcionales desde el cliente FTP.

=evidencias==================================================================================================

Eejcucion del script de automatización.
Creación y validación de usuarios.
Visualización de estructura de directorios desde FTP.
Pruebas de acceso y navegación en carpetas.
Pruebas de escritura en carpetas permitidas.
Cambio de grupo de usuario mediante re-ejecución del script.
Acceso anónimo restringido a la carpeta pública.
Validación de que el usuario anónimo no tiene permisos de escritura.

=problemas===================================================================================================

Error en nombres de funciones dentro del script, lo que provocó fallos en la ejecución.
Duplicidad de funciones que generaba comportamientos inesperados.
Problemas iniciales con los permisos de directorios que impedían el acceso correcto vía FTP.
Error en la visualización de carpetas debido a una configuración incorrecta de anon_root.
El script sobrescribía configuraciones manuales, lo que requería ajustar directamente el código del script.
Dificultades en la implementación de bind mounts para reflejar correctamente la estructura de carpetas por usuario.
Confusión en la navegación dentro del FTP al no considerar el directorio raíz asignado.

==29-03-2026==================================================================
10:00pm

=5.Configuración de Windows Server================================================

actividades realizadas:

Se inició la preparación del entorno en Windows Server para la implementación del servicio FTP.
Se verificó la disponibilidad de los roles y características necesarias para IIS y FTP.
Se identificó el estado actual del servidor antes de realizar la instalación del servicio.
Se preparó el entorno para continuar con la configuración automatizada mediante PowerShell.

10:55
Se instaló el rol Web Server (IIS) junto con el servicio FTP mediante PowerShell.
Se incluyeron herramientas de administración para la gestión del servidor.
Se verificó la correcta instalación de los componentes necesarios para el servicio FTP.


=evidencias==================================================================================================

- Capturas de verificación de roles IIS y FTP.
- Capturas de comandos de PowerShell ejecutados en Windows Server.
-Captura de instalación FTP 
-Captura de servicios instalados de FTP

=6.Estructura de carpetas=================================================================

actividades realizadas:

Se creó la estructura base de directorios del servidor FTP en Windows Server.
Se definió la carpeta raíz C:\FTP y sus subdirectorios: general, reprobados, recursadores y usuarios.
Se utilizó PowerShell para automatizar la creación de las carpetas.
Se verificó la correcta generación de la estructura mediante comandos de sistema.


=evidencias================================================================================
Captura creación de estructura de carpetas
Captura de verificación de carpetas recién creadas


